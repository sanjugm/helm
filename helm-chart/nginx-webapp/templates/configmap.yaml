{{- if .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx-webapp.fullname" . }}-config
  labels:
    {{- include "nginx-webapp.labels" . | nindent 4 }}
data:
  default.conf: |
    {{- .Values.configMap.nginxConf | nindent 4 }}
{{- end }}
